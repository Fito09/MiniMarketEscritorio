<Window x:Class="WpfApp1.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inicio de Sesión" Height="250" Width="350" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Usuario:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="txtUsuario" Grid.Row="1" Height="25" Margin="0,0,0,10"/>
        <TextBlock Text="Contraseña:" Grid.Row="2" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="3" Height="25" Margin="0,0,0,10"/>
        <Button x:Name="btnIngresar" Content="Ingresar" Grid.Row="3" Height="30" Margin="0,48,0,-38" Click="btnIngresar_Click" Style="{StaticResource DefaultButtonStyle}"/>
    </Grid>
</Window>
